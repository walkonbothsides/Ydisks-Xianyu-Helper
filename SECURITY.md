# 安全策略

Ydisks 闲鱼助手会处理账号登录状态、Cookie、Token、管理员密码、数据库连接信息、AI 或通知
渠道凭证，以及订单和卡密等敏感数据。请先确认你有权测试相关账号和环境，并遵守适用的法律、
平台规则和账号授权范围。

## 支持范围

优先处理以下问题：

- Cookie、Token、密码、API 密钥或通知凭证可能泄露；
- 敏感数据进入日志、错误响应、前端状态、导出文件或公开构建产物；
- 管理员认证、账号隔离、所有权校验或权限边界被绕过；
- 数据库迁移、备份、恢复或删除行为造成未授权的数据暴露或破坏；
- 可在本地或受控环境稳定复现的远程代码执行、命令注入、路径穿越、跨站脚本、请求伪造或拒绝服务问题；
- 发布包、Docker 镜像或构建流程被篡改。

单纯的闲鱼接口变化、账号风控、验证码触发或平台服务中断，通常属于兼容性或运行问题；仍欢迎
通过普通 Issue 反馈，但不要在公开内容中提供绕过平台限制、未授权访问或批量滥用的方法。

## 报告方式

请通过 GitHub 私下联系维护者 [@Christ9038](https://github.com/Christ9038)，标题建议包含
“安全问题”。报告应尽量包含：

- 受影响的版本、提交号或发布包；
- 影响范围和安全后果；
- 不包含真实秘密的最小复现步骤；
- 必要的脱敏日志、请求结构或测试代码；
- 已采取的临时缓解措施。

不要在公开 Issue、Pull Request、截图、聊天记录或测试失败输出中提交真实 Cookie、Token、密码、
API 密钥、Webhook、二维码、数据库文件、卡密、订单信息或个人数据。维护者需要这些信息时，会
通过私下渠道说明安全的提供方式。

## 处理流程

维护者会先确认报告是否与本项目有关，再评估影响范围、可利用条件和修复优先级。确认后的问题
会在修复、测试和发布后公开必要的说明；报告人的署名会在获得同意后保留。暂不承诺固定响应
时间或漏洞奖励。

在修复完成前，请不要公开复现细节、利用代码或受影响部署信息。维护者可能要求提供更多脱敏
证据，也可能将无法复现、重复或不属于本项目的问题转为普通 Issue 处理。

## 使用者的应急措施

如果怀疑凭证已经泄露：

1. 立即停用受影响账号和自动化任务；
2. 在闲鱼官方流程或对应服务后台撤销并重新生成凭证；
3. 更换管理员、数据库、AI、SMTP 和通知渠道密码或密钥；
4. 保存必要的脱敏日志和时间线，不要继续传播原始凭证；
5. 使用受控环境验证修复，再恢复生产服务。

## 安全设计说明

项目会对持久化的敏感字段进行加密，并对日志和通知中的敏感内容进行脱敏；这不代表部署环境
天然安全。部署者仍需限制管理端口、保护 `XIANYU_DATA_KEY`、备份数据库和浏览器数据，并为
不同环境使用独立凭证。
