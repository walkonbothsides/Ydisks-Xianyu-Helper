# 账号、库存与商品（中文）

[English](Accounts-Inventory-and-Products) | 中文

本页是上线前的串联检查。按页面逐项操作时，可直接阅读：[账号管理](账号管理.zh-CN)、[卡密库存](卡密库存.zh-CN)、[商品列表/商品管理](商品管理.zh-CN)和[发货模板](发货模板.zh-CN)。

## 1. 接入账号

1. 进入“账号管理”，选择添加账号/扫码登录。
2. 用目标闲鱼账号在官方客户端完成扫码；若出现人脸或安全验证，按页面提示在官方流程中完成。
3. 等待后台保存登录信息，确认昵称、账号 ID 和运行状态正常。若状态显示“运行状态冲突”，表示数据库已停用但旧运行实例仍存活；不要把它当作已停用，等待或重试停用并检查服务日志。
4. 为账号填写备注，按需开启或暂停账号，并进入编辑页绑定通知渠道。
5. 首次接入后同步一次商品；开始自动化前确认账号处于启用状态。

不要手工向他人发送 Cookie。登录态、指纹和密码等敏感值由系统加密存储，依赖固定的 `XIANYU_DATA_KEY`。

## 2. 创建卡密库存

付款自动发货和评价赠品都先依赖卡密库存。进入“卡密库存”后点击“添加新卡密”：

| 类型 | 适用内容 | 填写方式 |
| --- | --- | --- |
| 文本 | 固定说明、链接 | 填写文本内容。 |
| 批量卡密（`data`） | 一次性兑换码、序列号 | 每行一个卡密；发货会按库存消费。 |
| 图片 | 固定图片资源 | 填写公开可访问的图片 URL。 |
| API | 外部发码接口 | 填写 URL、GET/POST、超时、请求头、参数和响应路径；先用无敏感测试数据验证。 |

通用字段包括名称、描述、启用状态和默认延时秒数。保存后复制“卡密组 ID”；它是自动化规则和批量铺货表格引用库存的唯一标识。

API 卡支持 `{order_id}`、`{item_id}`、`{buyer_id}`、`{chat_id}`、`{account_id}`、`{cookie_id}`、`{spec_name}`、`{spec_value}`、`{quantity}`、`{order_quantity}`、`{amount}`、`{order_amount}`、`{item_detail}`、`{trigger_type}`、`{timestamp}`、`{delivery_unit_index}`、`{delivery_total_count}` 和 `{idempotency_key}`。变量只替换请求头和参数值，不替换 URL 或请求头名称；GET 将参数放入 query，POST 使用 JSON body。响应路径可写成 `data.card.code` 或 `data.cards[0].code`，为空时系统会按 `data`、`content`、`card` 顺序提取。

请求头和参数会整体加密保存，列表和规则接口只返回是否已配置，不回显密钥明文。编辑时“保留”不会覆盖旧模板，“替换”写入新模板，“清除”删除对应模板。启用幂等重试前必须在请求头或参数中显式写入 `{idempotency_key}`；最多请求 3 次，只重试网络错误、超时、408、429 和 5xx。

管理员可在系统设置开启“用户配置 HTTP 只能访问公网”。开启后 API 发货、AI、HTTP 通知、远程图片和远程滑块服务不能访问本机、内网、链路本地、保留或云元数据地址，并会在 DNS 解析和重定向时重复校验。该开关默认关闭且保存后立即生效；不要把它当作 API 密钥或请求模板的替代安全措施。

批量卡密可用“批量导入”创建，也可选择已有 `data` 类型卡密组追加新卡密。上架前检查库存余量；库存不足时应先补货，不要依赖人工补救。

## 3. 发货模板

进入“发货模板”后，可以创建、编辑、启用或停用包含多条顺序消息的模板。模板支持订单、买家、卡密和自定义变量；在自动化规则中选择模板后，需要为每个卡密变量绑定库存，并填写规则要求的自定义变量。发货模板只支持付款后自动发货和评价后发送赠品，不支持拍下改价或求评价。仍被规则引用的模板不能删除，模板消息会按顺序独立发送。

## 4. 同步或发布商品

进入“商品管理”：

- 对已在闲鱼上存在的商品，选择同步全部或按页同步，确认商品归属到正确账号。
- 对单个新商品，使用发布入口填写标题、描述、价格、库存、邮费和图片。图片必须可用；提交前复核目标账号。
- 单商品发布支持最多 2 个规格类型；每种规格至少填写 2 个规格值，系统会自动生成规格组合，可分别设置每个组合的价格和库存。
- 规格值可以绑定规格图片；商品主图支持继续添加、删除、拖拽或调整顺序，第一张图片作为主图。
- 批量铺货的默认类目可以留空，也可以通过关键词从闲鱼获取。优先级为逐行类目、页面默认类目、自动识别，最后才使用“电子资料”兜底。
- 批量铺货商品按虚拟商品发布，不依赖实物发货地址；类目兜底不会改变付款后的虚拟交付逻辑。
- 商品发布成功不等于已开启自动发货。仍需在“自动化规则”中按商品/规格绑定卡密组，或在批量铺货表格中一并声明自动化配置。

## 5. 上线前检查

1. 账号在线并已启用。
2. 商品已同步或发布成功，价格、库存和图片正确。
3. 批量铺货的预检明细已显示正确的类目策略。
4. 所需卡密组启用且余量充足。
5. 自动化规则准确匹配商品；有规格时逐个规格确认。
6. 先用测试订单验证，检查订单列表、自动化异常列表和通知记录。
